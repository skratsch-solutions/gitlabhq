{
  "id": "gitlab.cd.argo_rollouts",
  "version": "0.0.0",
  "configuration": {
    "environment": "schemas/environment.json",
    "service_environment": "schemas/service_environment.json"
  },
  "steps": {
    "supported": "schemas/steps.json"
  },
  "scripts": {
    "deploy": "scripts/deploy.star"
  }
}
