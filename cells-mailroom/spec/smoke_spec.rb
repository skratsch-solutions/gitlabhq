# frozen_string_literal: true

# Placeholder spec that verifies the RSpec harness runs in CI. The service's
# runtime classes and their specs land in a follow-up merge request.
RSpec.describe 'cells-mailroom test harness' do
  it 'runs' do
    expect(true).to be(true)
  end
end
